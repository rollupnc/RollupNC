const RollupNC = artifacts.require("RollupNC");
const TestToken = artifacts.require("TestToken");

/*
    Here we want to stress the smart contract with large data sets and verify its behavior and efficeny under those conditions
*/ 

contract("StressTset", async accounts => {
  it("should correctly handle large numbers of deposits", async () => {
  });
});